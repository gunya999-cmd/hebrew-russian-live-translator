import React from 'react';
import ReactDOM from 'react-dom/client';
import SyncApp from './SyncApp';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(React.createElement(SyncApp));
